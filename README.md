# Aiden Wu — Frontend Developer Résumé

在线简历 / Live résumé: 部署于 GitHub Pages。

单文件静态页面，纯 HTML + CSS，**无构建步骤、无依赖、无第三方脚本与统计代码**。

- 直接打开 `index.html` 即可查看
- 浏览器打印（Ctrl / Cmd + P）即为 A4 两页版式，边距与分页已在 `@page` 中固定
- 移动端按 794px 布局视口整页缩放，无需横向滚动
